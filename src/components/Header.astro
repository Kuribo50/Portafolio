    <!-- Header -->
    <header class="fixed top-0 left-0 right-0 z-50">
      <!-- Barra principal del header con borde negro -->
      <div class="bg-background border-b border-black">
        <div class="max-w-6xl mx-auto px-6 py-5 flex items-center justify-between">
          <a
            href="/"
            class="text-2xl font-medium tracking-tight hover:text-accent transition-colors"
          >
            martinberoiza<span class="text-accent">.</span>
          </a>

          <!-- Menú para Desktop -->
          <nav class="hidden md:block">
            <ul class="flex space-x-12">
              <li>
                <a
                  href="#Inicio"
                  class="text-text hover:text-accent transition-colors text-sm tracking-widest"
                >
                  INICIO
                </a>
              </li>
              <li>
                <a
                  href="#proyectos"
                  class="text-text hover:text-accent transition-colors text-sm tracking-widest"
                >
                  PROYECTOS
                </a>
              </li>
              <li>
                <a
                  href="#sobre-mi"
                  class="text-text hover:text-accent transition-colors text-sm tracking-widest"
                >
                  SOBRE MI
                </a>
              </li>
            </ul>
          </nav>

          <!-- Botón del Menú para Mobile -->
          <button
            id="menu-toggle"
            class="md:hidden text-text hover:text-accent transition-colors"
            aria-label="Abrir menú"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor"
              class="w-6 h-6"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"
              />
            </svg>
          </button>
        </div>
      </div>

      <!-- Menú Móvil Desplegable con borde superior negro -->
      <div id="mobile-menu" class="hidden md:hidden">
        <nav class="bg-background border-t border-black">
          <ul class="px-6 py-5 flex flex-col space-y-4">
            <li>
              <a
                href="/"
                class="text-text hover:text-accent transition-colors text-lg tracking-tight"
              >
                INICIO
              </a>
            </li>
            <li>
              <a
                href="#proyectos"
                class="text-text hover:text-accent transition-colors text-lg tracking-tight"
              >
                PROYECTOS
              </a>
            </li>
            <li>
              <a
                href="#sobre-mi"
                class="text-text hover:text-accent transition-colors text-lg tracking-tight"
              >
                SOBRE MI
              </a>
            </li>
          </ul>
        </nav>
      </div>
    </header>
    <!-- Script para controlar el despliegue del menú móvil -->
    <script>
      const menuToggle = document.getElementById("menu-toggle");
      const mobileMenu = document.getElementById("mobile-menu");

      function toggleMenu() {
        mobileMenu.classList.toggle("hidden");
      }

      menuToggle.addEventListener("click", toggleMenu);
    </script>
  </body>
</html>
