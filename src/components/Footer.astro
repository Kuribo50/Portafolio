---
const currentYear = new Date().getFullYear();

const socialLinks = [
  { name: 'GITHUB', url: 'https://github.com' },
  { name: 'LINKEDIN', url: 'https://linkedin.com' }
];
---

<footer class="relative py-24 mt-32 border-t border-text/10 overflow-hidden bg-white">
  <div class="max-w-4xl mx-auto px-4">
    <!-- Sección de contacto -->
    <div class="relative z-10 flex flex-col md:flex-row justify-between items-center gap-12 mb-24">
      <div class="space-y-4 text-center md:text-left">
        <h2 class="text-4xl md:text-5xl font-bold tracking-tight text-text">
          TRABAJEMOS<br />JUNTOS<span class="text-accent">.</span>
        </h2>
        <p class="text-text/60 max-w-md leading-relaxed">
          Estoy disponible para nuevos proyectos y colaboraciones. ¡Hablemos!
        </p>
      </div>
      
      <div class="space-y-6 text-center">
        <div class="space-y-2">
          <h3 class="text-sm tracking-[0.2em] text-secondary font-medium">REDES SOCIALES</h3>
          <div class="flex flex-col gap-3">
            {socialLinks.map(link => (
              <a 
                href={link.url}
                class="group flex items-center gap-2 text-text/80 hover:text-accent transition-colors"
              >
                {link.name}
                <svg 
                  width="14" 
                  height="14" 
                  viewBox="0 0 24 24" 
                  fill="none" 
                  stroke="currentColor" 
                  stroke-width="2"
                  class="transform group-hover:translate-x-1 transition-transform duration-300"
                >
                  <path d="M7 17L17 7M17 7H7M17 7V17" />
                </svg>
              </a>
            ))}
          </div>
        </div>

        <a 
          href="mailto:contact@example.com"
          class="group inline-flex items-center gap-3 px-8 py-4 bg-accent text-text rounded-xl hover:bg-accent/90 transition-all duration-300 text-lg shadow-lg hover:shadow-xl hover:-translate-y-0.5"
        >
          Contáctame
          <svg 
            width="18" 
            height="18" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            stroke-width="2"
            class="transform group-hover:translate-x-1 transition-transform duration-300"
          >
            <path d="M7 17L17 7M17 7H7M17 7V17" />
          </svg>
        </a>
      </div>
    </div>

    <!-- Sección inferior del Footer -->
    <div class="relative z-10 flex flex-col md:flex-row justify-between items-center gap-6 pt-12 border-t border-text/10">
      <h2 class="text-3xl md:text-4xl font-bold text-text">
        Martin Beroiza<span class="text-accent">.</span>
      </h2>
      <div class="flex flex-col md:flex-row items-center gap-4 text-sm text-text/60">
        <span>© {currentYear}. All rights reserved.</span>
        <span class="hidden md:inline">•</span>
        <span>Concepción, Chile</span>
      </div>
    </div>

    <!-- Decoración de fondo -->
    <div class="absolute inset-0 opacity-5 pointer-events-none">
      <div class="absolute top-0 left-0 w-64 h-64 rounded-full bg-accent blur-3xl"></div>
      <div class="absolute bottom-0 right-0 w-64 h-64 rounded-full bg-secondary blur-3xl"></div>
    </div>
  </div>
</footer>
